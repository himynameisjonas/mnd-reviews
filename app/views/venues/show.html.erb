<% content_for :header do -%>
    <script type="text/javascript"
        src="http://maps.google.com/maps/api/js?sensor=false">
    </script>
    <script type="text/javascript">
        $(document).ready(function() {
            initialize_map('<%= @venue.adress %>');
        });
    </script>
<% end -%>
<p id="notice"><%= notice %></p>

<h1><%= @venue.name %></h1>
<%- if @venue.foursquare_id.present? -%>
    <iframe id="foursquare" src="http://foursquare.com/button.html?vid=<%= @venue.foursquare_id %>&color=light" allowtransparency="true" frameborder="0" scrolling="no" style="width:165px; height:25px;"></iframe>
<%- end -%>
<div id="map_canvas"></div>
<div id="adress">
    <p>
      <b>Adress:</b>
      <%= @venue.adress %>
    </p>

    <p>
      <b>Typ av mat:</b>
      <%= @venue.type_of_food %>
    </p>

    <p>
      <b>Prisklass:</b>
      <%= @venue.price %>
    </p>
    <%= link_to 'Redigera information', edit_venue_path(@venue) %>
</div>


<h2>Recensioner</h2>

<table>
  <tr>
    <th>Omdöme</th>
    <th>Övrigt</th>
    <th>Betyg (1-5)</th>
    <th>Rapporterad av</th>
    <th>Testad den</th>
    <th></th>
    <th></th>
  </tr>

<% @reviews.each do |review| %>
  <tr>
    <td><%= review.review %></td>
    <td><%= review.other %></td>
    <td><%= review.rating %></td>
    <td><%= review.author %></td>
    <td><%= review.date %></td>
    <td><%= link_to 'Redigera', edit_venue_review_path(@venue,review) %></td>
    <td><%= link_to 'Radera', venue_review_path(@venue,review), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Ny recension', new_venue_review_path(@venue) %> |
<%= link_to 'Tillbaka', venues_path %>